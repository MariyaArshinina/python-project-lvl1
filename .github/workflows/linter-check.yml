name: linter-check
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2      
      - name: install
        run: |
          make install
        env:
                CI: true
      - name: Run Linter
        run: |        
          make lint
